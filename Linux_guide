hostnamectl
df -h  -kh
fdisk -l
useradd
passwd
usermod -aG wheel your_user_id
groups username
su username
sudo passwd username

su - username
groups
sudo whoami
sudo ls -la /root
vim /etc/sysconfig/network-scripts/ifcfg-(ethernet)

hostnamectl
echo RHEL8.2_244_DEV > /etc/hostname
systemctl restart systemd-hostnamed

find / -type f -name "*.conf"
find / -type f -name "*_env"
find /etc -type f -name “*.conf”

rpm -qa | grep -i vnc
vncpasswd
vncserver


killall java
rpm -ivh PackageName.rmp


docker build -t ro-image .
docker container prune
docker rmi -f
docker images
docker images prune  
docker container prune or podman rm -f -a
docker system prune
ps -A |grep java
kill -9 PID




kubectl version --short

kubectl get pods
kubectl apply -f sitecontroller_hack.yaml
kubectl get pods
kubectl delete -f sitecontroller_hack.yaml
kubectl get pods -o wide
kubectl describe pod sitecontroller-hack-830-5c49b6fb87-qf7jv > /tmp/830trouble.txt
kubectl delete pod myapp

kubectl get pods --all-namespaces
kubectl delete --all deployments --namespace=devopsns


apiVersion: apps/v1
kind: Deployment
metadata:
  name: sitecontroller-hack-830
spec:
  selector:
    matchLabels:
      run: sitecontroller-nav-830
  template:
    metadata:
      labels:
        run: sitecontroller-nav-830
    spec:
      containers:
        - name: sitecontroller-hack-nav-830
#          image: docker.io/sc_hack:latest
#          image: docker.io/sc_hack:830

#	  image: docker.io/sc_hack:830:830
          image: docker.io/sc_hack:830:830
          image: docker.io/sc_hack:830:830

          imagePullPolicy: Always
          args: ["/bin/bash", "10.249.252.166"]
          volumeMounts:
          - mountPath: "/scdata"
            name: sc-vol
      nodeName: devopskubnode01
      volumes:
        - name: sc-vol
          persistentVolumeClaim:
            claimName: sc-volclaim-auto
      securityContext:
        runAsUser: 0
      imagePullSecrets:
        - name: kubsec
https://www.linkedin.com/pulse/ansible-roles-configure-loadbalancer-apache-web-server-agarwal


usermod -aG docker jenkins
usermod -aG root jenkins
chmod 664 /var/run/docker.sock
chmod 777 /var/run/docker.sock

jenkins ALL=(ALL) NOPASSWD: ALL
sudo docker build -t testimage .
sudo docker login -u naveen.victory@outlook.com -p Password
sudo docker push testimage:latest naveenraj123training/project_repo:v1

